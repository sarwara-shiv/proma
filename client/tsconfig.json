{
  "compilerOptions": {
    "target": "es5",                    // For wider browser compatibility
    "lib": [
      "dom",                             // Use DOM APIs
      "dom.iterable",                    // Enable iteration over DOM collections
      "esnext"                            // Use the latest ECMAScript features
    ],
    "allowJs": true,                     // Allow JavaScript files in the project
    "skipLibCheck": true,                // Skip type checking of declaration files (can improve performance)
    "esModuleInterop": true,             // Enable ES module interoperability
    "allowSyntheticDefaultImports": true,// Allow default imports from modules with no default export
    "strict": true,                      // Enable all strict type-checking options
    "forceConsistentCasingInFileNames": true, // Ensure consistent casing in file names
    "noFallthroughCasesInSwitch": true,  // Prevent fall-through behavior in switch statements
    "module": "esnext",                  // Use the latest ECMAScript module system
    "moduleResolution": "node",          // Resolve modules like Node.js (including support for `node_modules`)
    "resolveJsonModule": true,           // Allow importing JSON files as modules
    "isolatedModules": true,             // Ensures each file is treated as an isolated module (useful with `create-react-app`)
    "noEmit": true,                      // Don't emit any output (useful for type checking without generating .js files)
    "jsx": "react-jsx",                  // Use the new JSX transform (for React 17+)
    "baseUrl": "src",                    // Set the base directory for module resolution
    "paths": {
      "@/*": ["*"],  
      "@/context/*": ["context/*"]
                    
    },
    "typeRoots": [
      "./node_modules/@types",           // Default type definitions
      "./src/types",                     // Custom types you define in the "src/types" folder
      "./src/@types"                     // Custom types in the "src/@types" folder (optional)
    ]
  },
  "include": ["src/**/*"],
  "exclude": [
    "node_modules"                       // Exclude the "node_modules" directory
  ]
}
